# This file can be used for local development overrides
# Copy this to docker-compose.override.yml and customize as needed

version: "3.9"

services:
  app-dev:
    environment:
      - DEBUG=app:*
      - LOG_LEVEL=debug
    volumes:
      # Add additional local volumes here
      - ~/.ssh:/root/.ssh:ro  # SSH keys for git operations